export enum ThemePrefix {
    orange = 'orange',
    strawberry = 'strawberry',
  }
  
  export enum ThemeInfix {
    dark = '-dark',
    light = '-light',
  }
  
  export enum ThemeSuffix {
    theme = '-theme',
  }
  